name: flame-megatron-a100
channels:
  - conda-forge
dependencies:
  # Python/build tools only. CUDA-enabled PyTorch is installed by
  # scripts/miscellaneous/install_a100_env.sh from the official cu124 index.
  - python=3.10
  - pip=24.3.1
  - setuptools=75.6.0
  - wheel=0.45.1
  - git
  - cmake=3.30
  - ninja=1.11
  - make
  - pkg-config
